# ─── Server ───────────────────────────────────────────────
PORT=3000
ALLOWED_ORIGINS=*

# ─── Database – Primary ($con in PHP) ─────────────────────
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASS=rootpwd
DB_NAME=tejagro_sales_login

# ─── Database – BDM ($connect in PHP) ─────────────────────
# If both connections point to the same DB, copy the values above here.
DB_BDM_HOST=127.0.0.1
DB_BDM_PORT=3306
DB_BDM_USER=root
DB_BDM_PASS=rootpwd
DB_BDM_NAME=tejagro_bdm_login

# ─── SMS API (InfyReachConnect) ────────────────────────────
SMS_API_URL=https://web.infyreachconnect.in/api.php
SMS_API_USERNAME=tejagrotech
SMS_API_KEY=Gygll3wn82mZ
SMS_TEMPLATE_ID=1207176164603545432
SMS_SENDER_ID=TEJAGR

# ─── JWT ──────────────────────────────────────────────────
# TODO: Replace with a strong random secret (openssl rand -hex 64) once the PHP
# team is ready to roll out a new shared secret. Keeping the legacy PHP value
# here so existing PHP endpoints can decode tokens without code changes.
JWT_SECRET=owt123
JWT_ISSUER=localhost
JWT_AUDIENCE=myusers

# ─── OTP ──────────────────────────────────────────────────
OTP_EXPIRY_MINUTES=30

# Test bypass – this mobile skips SMS and always gets TEST_OTP
TEST_MOBILE=9168681342
TEST_OTP=1234
